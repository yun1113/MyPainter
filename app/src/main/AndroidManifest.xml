<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.painter"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-sdk
        android:minSdkVersion="21"
        android:targetSdkVersion="21" />

    <android:uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="18" />
    <android:uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <android:uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="18" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <!-- android:theme="@style/Theme.AppCompat" > -->
        <!-- <activity -->
        <!-- android:name=".Friendlist" -->
        <!-- android:label="@string/title_activity_friendlist" > -->
        <!-- <intent-filter> -->
        <!-- <action android:name="android.intent.action.MAIN" /> -->


        <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
        <!-- </intent-filter> -->
        <!-- </activity> -->
        <!-- <activity -->
        <!-- android:name=".Listactivity" -->
        <!-- android:label="@string/title_activity_listactivity" > -->
        <!-- </activity> -->
        <activity
            android:name=".LogInActivity"
            android:label="@string/app_name">
            <!--android:name=".Friends"-->
            <!--android:label="@string/title_activity_friends">-->

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".PersonalSetting"
            android:label="@string/title_activity_personal_setting"></activity>
        <activity

            android:name=".Gallery"
            android:label="@string/title_activity_gallery"></activity>
        <activity
            android:name=".MultipleConnection"
            android:label="@string/title_activity_multiple_connection"></activity>
        <activity
            android:name=".Sign_up"
            android:label="@string/title_activity_sign_up"></activity>
        <activity
            android:name=".Friends"
            android:label="@string/title_activity_friends"></activity>
        <!--android:name=".LogInActivity"-->
        <!--android:label="@string/app_name"></activity>-->

        <activity
            android:name=".GalleryDetail"
            android:label="@string/title_activity_gallery_detail"></activity>
    </application>

</manifest>
